const initialState = {
    feched: false,
    loading: false,
    error: '',
    username: null,
    userid: null,
}

export const dashboardReducer = (state = initialState, action) => {
    switch (action.type) {
        default:
            return state
    }
}